# ── Flask Security ─────────────────────────────────────────────
# Generate a new key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=796c27ab121f07b6870569604d4c9c16afb0dce67e6b0bdeafe71a2f7aa6b4fe

# ── Email Notification (Video Processing Completion) ──────────
# To enable: Go to Google Account → Security → App Passwords
# Create one for "AquaVision" and paste the 16-char code below.
# If these are empty or missing, email notifications are silently disabled.
MAIL_SENDER=
MAIL_APP_PASSWORD=